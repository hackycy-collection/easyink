/**
 * Four-side border visibility state.
 * Each side defaults to `true` (visible) when `undefined`.
 */
export interface BorderSides {
  top?: boolean
  right?: boolean
  bottom?: boolean
  left?: boolean
}
